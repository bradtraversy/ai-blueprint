# Project Name

A description of your project and the problem it solves.

> **This project uses a workflow layer (the AI Coding Blueprint), not an app
> skeleton.** To start a new project, scaffold the app first in an empty folder
> (create-next-app, Vite, etc.), then overlay these files on top. Never run a
> framework scaffolder inside a directory that already holds the blueprint files
> (CLAUDE.md, context/, docs/, .claude/); it fails because the directory isn't empty.

> **New here?** Read [README.md](README.md); it explains the whole workflow: the
> two files you write, the skills that generate the rest, and how to build feature
> by feature.

## Context Files

Read these to get the full context of the project:

- @context/project-overview.md - the project's source of truth (generated by `/overview`)
- @context/coding-standards.md - conventions to follow
- @context/ai-interaction.md - how to work with me on this project
- @context/current-feature.md - the one feature being built right now (generated by `/feature`)

## Commands

For a standard Next.js project. Change or remove if you're using something else.

- **Dev server**: `npm run dev` (http://localhost:3000)
- **Build**: `npm run build`
- **Production server**: `npm run start`
- **Lint**: `npm run lint`
- **Test**: `npm run test` (single run)
- **Test watch**: `npm run test:watch`
